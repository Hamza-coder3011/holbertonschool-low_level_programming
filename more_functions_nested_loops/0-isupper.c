#include <stdio.h>
/**
* @c: character to check
*
* main - Fonctions that cheks for uppercase character
*
* Return: gives 1 or 0
*/
int main(void)

int _isupper(int c);
{
	if (c >= 'a' && c <='z')
	{
		return (1);
	}
	return (0);
}
